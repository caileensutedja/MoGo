<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MogoApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MoGo"
        tools:targetApi="31" >

        <activity
            android:name=".UIScreen.OfferScreen.OfferScreen"
            android:exported="false"
            android:label="@string/title_activity_offer_screen"
            android:theme="@style/Theme.MoGo" />

        <activity
            android:name=".UIScreen.SignInScreen.SignInScreen"
            android:exported="false"
            android:label="@string/title_activity_log_in_screen"
            android:theme="@style/Theme.MoGo" />

        <activity
            android:name=".UIScreen.RegisterScreen.RegisterScreen"
            android:exported="false"
            android:label="@string/title_activity_register_screen"
            android:theme="@style/Theme.MoGo" />

        <activity
            android:name=".UIScreen.WelcomeScreen.WelcomeScreen"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MoGo" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>
